"use client";

/**
 * UI-layer ResourceGrid.
 * Wraps the feature ResourceGrid with the canonical 4-column layout grid.
 * Import this in pages; import from @/components/resources/ResourceGrid only
 * when you need the low-level component directly.
 */
export {
  ResourceGrid,
  RESOURCE_GRID_CLASSES,
} from "@/components/resources/ResourceGrid";
export { ResourceCardSkeleton } from "@/components/resources/ResourceCard";
